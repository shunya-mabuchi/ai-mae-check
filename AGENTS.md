# AGENTS.md

## プロジェクト概要

AIまえチェックは、ChatGPT、Claude、Geminiなどへ文章を送る前に、個人情報・秘密情報・APIキー・社外秘らしい内容の消し忘れに気づくChrome拡張です。ルールベース検出を主役とし、ブラウザ内の小型日本語モデルを補助的な文脈チェックに使います。

## 絶対に守る方針

- UI、README、ドキュメント、コメントは基本的に日本語で書く。
- 外部LLM APIを使わない。
- ユーザー本文を開発者サーバーやバックエンドへ送らない。
- ユーザーの貼り付け本文、送信本文、ファイル本文、検出結果、placeholderMapを永続保存しない。
- ユーザー本文を `console.log`、エラー、診断ログへ出力しない。
- Analyticsやトラッキングを入れない。
- 保存してよいのは設定と検証済みの署名付き追加ルールキャッシュだけ。保存先は `chrome.storage.local`。
- 初期対象サイト以外に `<all_urls>` を無条件で要求しない。
- publicリポジトリのIssue、PR、README、テストデータに実在の個人情報、APIキー、トークンを入れない。

## 検出とAIの役割分担

- `packages/core` のルールベース検出を最優先にする。
- メール、電話番号、APIキー、JWT、秘密鍵、.env、Basic認証URLなど確定的な情報をAIの主役にしない。
- 高リスクや秘密情報保護対象の送信可否は、ルールとポリシーで扱う。
- 小型NERモデルは、人名・組織名・場所・施設・製品・イベントなどの実体候補を提示する。
- Ruri-v3-30m ONNXは、契約・人事・法務・財務・社内・未公開などの文脈候補を提示する。
- AIは候補提示専用であり、完全な安全判定、最終的な送信許可、文章生成には使わない。
- AI経路が失敗しても、ルールベース検出は利用できるようにする。
- 現行のAI経路はTransformers.js + ONNX Runtime WebのCPU/WASMのみ。WebLLM、WebGPU、生成プロンプト、LLM出力のJSON解析は現行機能ではない。

## プライバシー

- 初回利用時に第三者のモデル配信元からモデルファイルを取得し、ブラウザキャッシュを利用する場合がある。
- モデル取得と本文送信は別の処理であり、ユーザー本文は外部LLM APIや開発者サーバーへ送信しない。
- モデル配信元、ブラウザの保存容量、端末メモリ、CPU命令、ネットワーク制限には依存する。

## 実装責務

- 検出エンジン、マスキング、ポリシー判定は `packages/core` に集約する。
- ローカルAIのモデル定義、NER、文脈分類、Worker、エラー処理は `packages/llm` に集約する。
- サイト固有のDOM操作は各adapterに集約し、検出エンジンへ混ぜない。
- 型安全性を重視し、`any` は使わない。

## 開発とGitHub運用

- 作業はIssueを起点にし、Issue番号を含むブランチを作る。
- 変更はPRにまとめ、確認後にマージする。
- 実装後は必要に応じて `pnpm lint`、`pnpm test`、`pnpm typecheck`、`pnpm build` を実行する。
- 実サイトの本文や秘密情報はテストに使わず、ダミー文を使う。
